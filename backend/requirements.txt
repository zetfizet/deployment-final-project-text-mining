# Web framework + ASGI server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ML inference — PyTorch (CPU-only untuk deployment ringan)
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1
--extra-index-url https://download.pytorch.org/whl/cpu

# HuggingFace Transformers + tokenizer
transformers==4.44.2
sentencepiece==0.2.0
tokenizers==0.19.1

# Data processing
pandas==2.2.3
numpy==1.26.4

# Request/response validation (Pydantic v2)
pydantic==2.8.2

# Upload file CSV dari frontend
python-multipart==0.0.9

# CORS + HTTP utilities
httpx==0.27.2

# Caching sederhana
cachetools==5.5.0
